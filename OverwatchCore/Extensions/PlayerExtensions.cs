using System;
using OverwatchCore.Data;
using OverwatchCore.StaticResources;

namespace OverwatchCore.Extensions
{
    public static class PlayerExtensions
    {
        public static Player Validate(this Player player, bool privateProfile = false)
        {
            // if (Validator.Validate(player.Username)) return null;
            // if (Validator.Validate(player.Aliases)) return null;
            // if (Validator.Validate(player.PlayerId)) return null;
            // if (Validator.Validate(player.ProfileUrl)) return null;
            // if (Validator.Validate(player.PlayerLevel)) return null;
            // if (Validator.Validate(player.PlayerLevelImage)) return null;
            // if (Validator.Validate(player.Prestige)) return null;
            // if (Validator.Validate(player.PrestigeImage) || player.Prestige % 6 == 0) return null;
            // if (Validator.Validate(player.CompetitiveRank)) return null;
            // if (Validator.Validate(player.EndorsementLevel)) return null;
            // if (Validator.Validate(player.EndorsementImage)) return null;
            // if (Validator.Validate(player.IsProfilePrivate)) return null;
            // if (Validator.Validate(player.GamesWon)) return null;
            // if (Validator.Validate(player.CompetitiveRankImageUrl)) return null;
            // if (Validator.Validate(player.ProfilePortraitUrl)) return null;

            // if (privateProfile) return player;

            // if (Validator.Validate(player.Endorsements)) return null;
            // if (Validator.Validate(player.CasualStats)) return null;
            // if (Validator.Validate(player.CompetitiveStats)) return null;
            // if (Validator.Validate(player.Achievements)) return null;
            
            return player;
        }
    }
}
